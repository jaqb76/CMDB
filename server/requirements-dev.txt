-r requirements.txt
pytest>=8.0
httpx>=0.27
Pillow>=10.0  # test rozmiarow favicon.ico

# quickstart.py wystawia certyfikat self-signed bez polecenia openssl,
# ktorego na Windows zwykle nie ma.
cryptography>=42.0
