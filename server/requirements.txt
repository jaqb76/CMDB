fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
jinja2>=3.1
python-multipart>=0.0.9
itsdangerous>=2.2
argon2-cffi>=23.1
psycopg[binary]>=3.2
# Szyfrowanie hasel SMTP - musi byc odwracalne, bo SMTP wymaga
# podania hasla przy kazdym polaczeniu.
cryptography>=42.0

# Kody QR na stronie instalacji (adres APK aplikacji Android). Czysty Python
# bez zaleznosci - portal pracuje takze w sieciach bez dostepu do internetu,
# wiec rysowanie kodu skryptem z CDN nie wchodzilo w gre.
segno>=1.6

# Eksport raportow helpdesku do XLSX. CSV robimy sami, ale arkusz to plik
# ZIP z kilkoma dokumentami XML - wlasna implementacja bylaby biblioteka.
openpyxl>=3.1

# Tresc artykulow bazy wiedzy (Markdown). Renderujemy z wylaczonym surowym
# HTML - znacznik wpisany w tresc zawsze wychodzi jako tekst. Czysty Python,
# bez zaleznosci systemowych i bez niczego z CDN.
markdown-it-py>=3.0
